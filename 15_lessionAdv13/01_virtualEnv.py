# 'pip freeze' returns all the package installed in a given python environment along with the versions.
# pip freeze > requirements.txt -> This cmd creates the 'requirements.txt' in the same directory containing the output of 'pip freeze'.

# To install them 
# pip install -r requirements.txt